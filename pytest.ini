[pytest]
addopts = -q --basetemp=.pytest-tmp -m "not hardware"
testpaths = tests
pythonpath = .
markers =
    hardware: tests that require physical ROV hardware
    integration: integration tests (no hardware required)
